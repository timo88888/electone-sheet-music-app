# エレクトーン楽譜生成アプリ 機能一覧（ベース）

## コンセプト
- PC用デスクトップアプリ（Electron、ローカル環境で完結）
- マウス操作で音符をクリック配置していく楽譜エディタ
- エレクトーン特有の3段譜表（上鍵盤・下鍵盤・ペダル）に対応

---

## MVP（最初に作る最小機能）

### 譜面編集
- [x] 3段譜表の表示（上鍵盤／下鍵盤／ペダル）
- [x] マウスクリックによる音符配置（クリック位置→音高決定）
- [x] 音符の種類選択パレット（全音符・2分音符・4分音符・8分音符・16分音符、付点、休符）
- [x] 配置済み音符のドラッグ移動・クリック削除
- [x] 小節の追加・削除
- [x] 複数ページ（譜面が長くなった場合の改ページ）

### 操作性
- [x] Undo / Redo
- [x] 拡大・縮小表示（ズーム）

### 再生・確認
- [x] MIDI再生によるプレビュー（音を鳴らして確認）
- [x] 内蔵音源・自然な演奏表現（簡易シンセ音源。本格的なHuman Playback相当は今後の課題）

### ファイル管理
- [x] ローカル保存・読み込み（JSON形式）
- [x] PDF書き出し／印刷（印刷ダイアログの「PDFとして保存」を利用）
- [x] MIDI／WAV書き出し（MP3は未対応、将来追加を検討）

---

## v2（実装済み）

- [x] 弱起（ピックアップ小節、任意の拍数を1小節目に設定可能）
- [x] レジストレーション記入欄（小節ごとにテキストを表示、クリックで編集）
- [x] コードネーム表記（小節ごとにテキストを表示、クリックで編集）
- [x] タイ・スラー、強弱記号（pp〜ff）・クレッシェンド/デクレッシェンドの入力
- [x] 繰り返し記号（リピート、D.C./D.S./Fine/Segno/Coda）— 再生・MIDI/WAV書き出しにも反映
- [x] テンプレート機能（拍子・小節数・弱起設定の空フォーマットを保存/読み込み）
- [x] 歌詞入力
- [x] 移調機能（小節範囲を選択して音階度数単位で移調。臨時記号非対応のため半音単位ではなく度数単位）

---

## v3（実装済み）

- [x] ツールバーのアイコン表記（音符・休符・タイ・スラー・クレッシェンド/デクレッシェンドなど）
- [x] 譜表レイアウトのはみ出し修正（行1列目の幅を調整してクレフ/拍子記号の分を確保）
- [x] 拍子記号の変更（4/4以外にも対応、いつでも変更可能）
- [x] 音部記号の選択（上鍵盤/下鍵盤/ペダルごとにト音記号/ヘ音記号を選択、デフォルトは下鍵盤・ペダルがヘ音記号）
- [x] 臨時記号（シャープ/フラット/ナチュラル）による半音（黒鍵）入力

---

## v4（実装済み）

- [x] 休符の全種類（全休符〜16分休符）をアイコンで選択可能に
- [x] 小節線（二重線・終止線）の追加
- [x] 2段目・3段目の休符位置ズレを修正（クレフごとの基準位置に対応、v3で下鍵盤/ペダルをヘ音記号にした際のバグ）
- [x] 三連符（自動グループ化・連桁・かっこ表示）
- [x] アーティキュレーション（スタッカート・テヌート・アクセント・マルカート）の追加・削除
- [x] 範囲選択（Shift+ドラッグ）によるコピー・ペースト・まとめて削除 ※v5でドラッグ方式を変更
- [x] 強弱記号などの配置を一般的な位置（譜表の下）に統一、音符選択時にリボンへ現在の装飾状態を反映
- [x] フローティングツールボックス（譜面クリックで表示、ドラッグ移動可、閉じるまで維持）
- [x] タブ+リボンによるUI再構成（ホーム/小節/記号/編集/出力）
- [x] 部分的にしか埋まっていない小節で音符が間延びして表示される問題を修正（Formatterの幅を充填率に応じて調整）

---

## v5（実装済み）

- [x] 範囲選択の解除（譜面や余白をクリックすると選択解除）
- [x] 範囲選択の操作方式変更（Shift不要、クリック&ドラッグ方式に）
- [x] 右クリックコンテキストメニュー（範囲選択時: コピー/ペースト/まとめて削除/タイ・スラー追加/クレッシェンド・デクレッシェンド追加）— Excel/Word同様の操作感
- [x] 曲タイトル・作詞・作曲者名を1ページ目に直接入力できるテキスト欄（タイトルはクイックアクセスバーと連動）
- [x] 新規作成時、最初のページ1枚分（16小節）をあらかじめ用意
- [x] 終止線を設定した小節以降を自動削除
- [x] 各小節上部に作業用の小節番号を表示（印刷/PDF出力時は非表示）。クリックで選択、右クリックで小節線/リピート/マーカー/レジストレーション・コードの編集メニュー

---

## v6（実装済み: バグ修正と土台固め）

Finaleマニュアルとの比較（[Finale比較・改善リスト.md](./Finale比較・改善リスト.md)）と、コード全体のレビュー（[問題点リスト.md](./問題点リスト.md)）を踏まえた修正。

### 記譜・データの正しさ
- [x] 拍子記号の分母を正しく扱う（6/8=3拍、2/2=4拍。従来は分子だけを見ていた）
- [x] 和音のタイが音の数だけ表示されるように（VexFlowのオプション名の誤りで、常に最低音の1本しか描かれていなかった）
- [x] 和音へのスラーを1本に正規化（音ごとに重ねて描かない）
- [x] タイが「直後の音符」にしか届かないように（従来は相手を編集すると数小節先の同じ高さの音符まで伸びていた）
- [x] 音符の削除・音高変更・コピー時にタイ/スラー/グリッサンドの整合性を保つ（`pushHistory()` に一元化）
- [x] 移調してもスラー・グリッサンドが消えない
- [x] 和音へのタイが同じ拍に重複した音符を作らない
- [x] 連符の解除（右クリック →「連符を解除」）
- [x] 拍子・弱起を変更したときに、入りきらない小節の詰め直し（Rebar）を確認
- [x] 終止線で以降の小節を削除する前に確認

### 印刷・書き出し
- [x] 印刷/PDFに楽譜だけを出力（タブ・リボン・小節番号・選択ハイライト・キャレットを除外）
- [x] A4（縦）の用紙1枚に譜面1ページがちょうど収まるように
- [x] MIDI書き出しに音色（プログラムチェンジ）・拍子・調号を含める
- [x] 保存ファイルに選択状態が混ざらないように

### 再生
- [x] 強弱記号（pp〜ff）をベロシティに反映（次の指示まで有効）
- [x] アクセント/マルカートで強く、スタッカートで短く
- [x] n番括弧（1番/2番…）を演奏順に反映
- [x] Coda（To Coda → コーダ）のジャンプに対応

### 操作性・その他
- [x] タイトルを変えたら1ページ目の表示も即反映
- [x] 休符はクリックだけでは音符に変わらない（選択できるように。連符の未入力枠は従来どおりクリックで入力）
- [x] 「開く」「テンプレートから新規作成」で未保存の変更を確認
- [x] テンプレート読み込みのクラッシュを修正
- [x] ズーム・ドラッグ中の全ページ再描画をやめて軽量化
- [x] 単体テストを追加（`npm test` / 64件）

---

## v7（実装済み: 入力方式の刷新と小節単位の管理）

### 入力方式
- [x] 1クリックで音符を配置（2回クリックで確定する入力キャレットを廃止）
- [x] 置いた音符をそのまま選択状態（青）で表示。次を置くと選択がそちらへ移る

### 小節単位の管理
- [x] 小節の容量を厳密に扱う。入りきらない音符は配置せず「◯小節目にはこれ以上音符を入れられません」と表示（後ろの小節への押し出しを廃止）
- [x] 挿入・ペースト・タイの自動追加・連符の解除、すべて同じ容量ルールで統一
- [x] 途中まで書いた小節の残りを、次の小節に音符を置いた時点で自動的に休符で補完

### n番括弧
- [x] 右端を閉じない鉤括弧の形に変更
- [x] 1つの括弧＝1小節に固定
- [x] 範囲選択ではなく「対象小節」1つに設定する方式へ（小節タブ／小節番号の右クリックメニュー）

### 再生
- [x] グリッサンドをMIDI再生に反映（開始音から終了音まで白鍵を順に滑らせる）

### 印刷・PDF
- [x] リボン・タブが印刷される問題を修正（`@media print` ではなく `html.printing` クラス方式に変更し、印刷とPDF書き出しの両経路で確実に適用）
- [x] 末尾に白紙のページが1枚追加される問題を修正

### その他のバグ修正
- [x] 譜表の当たり判定が譜表間隔に追従するように（加線が多い曲で反応しない帯ができる問題）
- [x] 1ページあたりの段数を譜表の高さから自動計算（加線やn番括弧でページ下端からはみ出す問題）
- [x] 編集しながら再生したときに再生位置ハイライトが古い座標に出る問題
- [x] 未使用のコード（コード記号パーサ、半音移調、未使用の定数群）を削除

---

## v8（実装済み: ファイル管理とタブ整理、Finale由来の記譜機能）

### ファイル管理
- [x] **上書き保存**（Ctrl+S）— 開いているファイルにそのまま保存。未保存なら保存先を聞く
- [x] **名前を付けて保存**（Ctrl+Shift+S）／**開く**（Ctrl+O）をOSのネイティブダイアログに変更
- [x] MIDI/WAV書き出しも保存先を選べるように（ダウンロードフォルダに溜まらない）
- [x] ウィンドウタイトルにファイル名を表示。未保存の変更があれば「●」を表示
- [x] Ctrl+P で印刷

### タブの整理
- [x] 「小節の追加・削除」を編集タブから小節タブへ移動
- [x] 範囲選択・ペーストのボタンをリボンから削除（右クリックメニューに集約）
- [x] n番括弧を小節番号の右クリックメニューからも設定可能に

### 記譜（Finale由来）
- [x] **8va / 8vb（オクターブ記号）** — 2点指定で範囲を囲み、再生でも1オクターブ移動
- [x] **符尾の向きの手動指定**（自動/上向き/下向き）
- [x] **連桁をここで分ける**（自動グループ化の手動上書き）

### バグ修正
- [x] Undo/Redo で選択が毎回外れる問題（音符が残っていれば選択を維持）
- [x] 和音の同じ高さに臨時記号付きでクリックしたとき、拒否ではなく臨時記号を変更するように
- [x] Undo履歴が無制限に増える問題（上限200手）
- [x] 壊れた/古い楽譜ファイルを開いたときのクラッシュ耐性（拍子・調号・音部記号などを検証して既定値で補完）

---

## 将来的な拡張（余裕があれば）

- [ ] MIDIファイルインポート → 自動採譜（音符の自動配置）
- [ ] 楽譜スキャン読み込み（既存の紙の楽譜から自動採譜、OMR）
- [ ] MusicXML形式の入出力（他の楽譜ソフトとの互換性）
- [ ] レジストレーションのプリセット管理（機種別など）
- [ ] 複数声部・オブリガートパートの追加
- [ ] クラウド同期・共同編集（オンライン化する場合のみ）

---

## 技術方針（決定済み）
- Electron（デスクトップアプリとして配布、ローカルで完結）
- 内部はWeb技術（HTML/CSS/JS、Canvas or SVGで譜面描画）
- 既存の記譜ライブラリ（VexFlowなど）をベースに、エレクトーン特有の3段譜表を追加実装する方針を検討中
