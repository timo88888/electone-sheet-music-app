# 現状の問題点リスト（v0.5.0 コードレビュー）

対象: `src/main/*.js`, `src/renderer/*.js`, `src/renderer/index.html`, `src/renderer/style.css`（全7,000行）

すべてコードを読んで確認したもの。各項目に**該当箇所**を付けてある。
分類: **A=バグ（誤動作・データ破損）／B=構成・設計上の問題／C=UX上の引っかかり**

---

# 対応状況（2026-08-15 時点）

以下の項目は**修正済み**。`npm test` に64件のテストを追加して再発を防いでいる。

| 項目 | 内容 | 検証 |
|---|---|---|
| **A-1** | 拍子の分母を無視していた（6/8が6拍、2/2が2拍） | テスト9件 |
| **A-2** | 拍子・弱起の変更後に容量を再チェックしない → 確認ダイアログ＋詰め直し（Rebar）を追加 | |
| **A-3** | タイが遠くの同名音に伸びる → 「直後の音符にしか届かない」ルールに変更 | テスト8件 |
| **A-4** | 削除経路ごとにリンク後始末が抜けていた → `pushHistory()` に一元化 | テスト5件 |
| **A-5** | 音高変更でスラー/グリッサンドが消える → 変更時にリンクを追従させる | テスト3件 |
| **A-6** | ペーストしたスラーが元の音符を指す → コピー範囲内で張り替え | テスト3件 |
| **A-7** | テンプレート読み込みでクラッシュ | |
| **A-8** | 連符を解除できない → 右クリックメニューに「連符を解除」を追加 | |
| **A-9** | n番括弧が再生に反映されない | テスト4件 |
| **A-10** | Codaが再生で無視される | テスト2件 |
| **A-11** | 和音タイが同じ拍に重複音符を作る（`partialChordNote`） | 実機確認 |
| **A-12** | 選択ハイライト・キャレットが印刷/PDFに出る | 実機確認 |
| **A-13** | タイトル変更が1ページ目に反映されない | |
| **A-14** | MIDI書き出しが音色・拍子・調号を落とす | テスト5件 |
| **A-15** | 強弱記号・アーティキュレーションが音に反映されない | テスト5件 |
| **A-19** | 休符がクリックしただけで音符に変わる | |
| **A-21** | 保存JSONに選択状態が混入する | |
| **A-22** | 挿入と音価変更で「あふれ」の挙動が食い違う → 送った件数を通知 | |
| **B-2** | ズーム・ドラッグで全ページを再描画 | |
| **B-4** | 開く/テンプレートが未保存確認をしない | |
| **B-8** | 印刷/PDFのページサイズ未指定 → A4に一致 | 実機計測 |
| **B-9** | テストが存在しない → `npm test`（64件） | |
| **C-2** | 終止線で確認なしに以降を全削除 | |

**追加した検証まわりの事実**

- **和音タイが1本しか出なかった真因**は VexFlow のオプション名だった。VexFlow 5 の `TieNotes` は `firstIndexes`/`lastIndexes`（e）だが、コードは `firstIndices`/`lastIndices`（i）を渡していた。`StaveTie.setNotes()` は知らないキーを黙って `[0]` に差し替えるため、**何本タイを付けても常に最低音の1本だけ**が描かれていた。重なっていたのではなく、そもそも1本しか描いていなかった。
- 同様に `Voice` のオプションも VexFlow 5 では `numBeats`/`beatValue`（camelCase）で、`num_beats`/`beat_value` は無視され、**全ての小節が常に4/4として整形されていた**。

**第2ラウンド（同日）で追加対応**

| 項目 | 内容 | 検証 |
|---|---|---|
| **A-16** | コード再生のデッドコード（`parseChordSymbol`）と未使用の定数群・`shiftSemitone`・`indexForPitch` を削除 | |
| **A-17** | 譜表の当たり判定が固定値で、譜表間隔を広げると死角ができる → 間隔に追従 | |
| **A-18** | 1ページ4段固定でページ下端からはみ出す → 段数を高さから自動計算 | |
| **A-20** | 編集中の再生ハイライトが古い座標を参照 → 描画時に位置を再取得 | |
| **A-22** | 挿入と音価変更で「あふれ」の挙動が食い違う → 全経路を容量エラーに統一 | |
| — | 入力方式を1クリック＋選択状態に変更、キャレットを廃止 | 実機確認 |
| — | 小節容量を厳密化（押し出し廃止）＋前小節の休符自動補完 | 実機確認 |
| — | n番括弧を鉤括弧・1小節・単一小節指定に変更 | 実機確認 |
| — | グリッサンドをMIDI再生に反映（白鍵） | テスト8件 |
| — | 印刷でリボン/タブが出る・末尾に白紙ページが出る問題 | 実機計測 |

**未対応（次回以降）**

B-1（`render()` の副作用）／B-3（上書き保存が無い）／B-6（`app.js` の分割）／B-7（履歴のメモリ）／B-10（壊れたファイルへの耐性）／C-1・C-3〜C-12

Finale比較リストの優先度A のうち未着手: A-3（キーボード入力の拡張）／A-1・A-2（拍子・調号の途中変更）／A-5（8va/8vb）／A-9（装飾音符）／A-10（記号の位置微調整）／A-21〜A-23（ステム・連桁の手動操作）

---

---

## 総評

土台（3段譜の描画、VexFlow連携、履歴、当たり判定）はよくできている。
問題が集中しているのは次の3か所:

1. **音符どうしのリンク（タイ／スラー／グリッサンド）の寿命管理** — 相手が消えたり音高が変わったときの後始末が経路ごとにバラバラで、指摘のあった不具合はほぼここに集約される
2. **拍子記号の扱い** — 分母が無視されており、4分音符以外を単位にする拍子はすべて容量が誤っている
3. **`render()` の責務過多** — 描画関数がスコアを書き換え、UI状態をモデルに書き込み、毎回全ページを作り直している

---

# A. バグ

## A-1. 拍子記号の**分母が完全に無視されている** 🔴最優先

```js
// scoreModel.js:274
export function beatsPerMeasure(score) {
  const [beats] = score.timeSig.split('/').map(Number);  // ← 分母を捨てている
  return beats;
}
```

`DURATION_BEATS` は4分音符=1の単位系。したがって:

| 拍子 | 正しい容量（4分音符換算） | 現状 |
|---|---|---|
| 4/4 | 4 | 4 ✅ |
| 3/4 | 3 | 3 ✅ |
| **6/8** | **3** | **6**（2倍） |
| **2/2** | **4** | **2**（半分） |
| **3/8** | **1.5** | **3** |
| **12/8** | **6** | **12** |

**影響:** 6/8で1小節に4分音符が6個入る。2/2では2分音符1個で満杯になる。
さらに `staffRenderer.js:870` の `new Voice({ num_beats: measureCapacity(score, m), beat_value: 4 })` も `beat_value` を4固定にしているので、描画も再生（`playback.js:119` の `time += capacity * secondsPerBeat`）も全部ズレる。

**修正:** `beatsPerMeasure` を `beats * (4 / denominator)` にする。`isValidTimeSig` は分母 1/2/4/8/16/32 を許しているので、全部が影響範囲。

---

## A-2. 拍子・弱起を変更しても既存小節の容量を再チェックしない

```js
// app.js:2898  btn-apply-timesig
score.timeSig = value;
// … pickupBeats のクランプだけして pushHistory(); render();
```

4/4で埋めた楽譜を3/4に変えても、各小節は4拍分の音符を抱えたまま。`cascadeOverflow` も呼ばれない。
`Voice.setStrict(false)` なので描画は落ちないが、小節線を無視して音符がはみ出し、再生は `capacity` 分しか進まないので次の小節と重なって鳴る。

弱起（`pickupInput`, app.js:2918）を減らしたときも同じ。

→ 前回の `Finale比較・改善リスト.md` §6 の「詰め直し（Rebar）」がまさにこれ。

---

## A-3. タイの相手が消える／変わると、**遠くの音符に勝手につながる** 🔴

タイは `tone.tieToNext`（真偽値）だけを持ち、相手は描画時に「同じ高さの次の音符」を探して決まる:

```js
// staffRenderer.js:963
if (tone.tieToNext) pendingTie[part].set(pid, {...});
```

そして `pendingTie` は**本物の休符に出会うまで永久に持ち越される**（staffRenderer.js:941）。

**再現:** C4にタイを付ける → 次の音符（C4）の音高をD4に変える → タイは消えず、**5小節先のC4まで伸びる**。

同じことが以下でも起きる:
- タイ相手の音符を削除する（`clearSlursTargeting` は `slurTo`/`glissandoTo` しか消さず、`tieToNext` は残る）
- 移調（A-5）
- 和音から1音だけ削除する（app.js:2119）

---

## A-4. 音符削除の**一部経路だけ**リンクの後始末をしている

`clearSlursTargeting(noteId)`（app.js:2101）を呼んでいるのは2か所だけ:

| 経路 | 該当箇所 | 後始末 |
|---|---|---|
| 単一削除 `deleteSelected` | app.js:2123 | ✅ |
| 音符範囲削除 `deleteSelectedNoteRange` | app.js:3428 | ✅ |
| **範囲まとめて削除 `doRangeDelete`** | app.js:3353 `= []` | ❌ |
| **小節削除 `btn-remove-measure`** | app.js:2569 `splice` | ❌ |
| **終止線で以降を切り捨て** | app.js:3206 `measures.length =` | ❌ |
| **和音から1音削除** | app.js:2119 `keys.splice` | ❌ |

❌の経路では `slurTo.noteId` が存在しない音符を指したまま残る。描画側は `targetKeyIndex === -1` で黙って描かないので（staffRenderer.js:694）、**スラーが理由なく消えたように見える**。保存ファイルにもゴミが残り続ける。

---

## A-5. 音高を変えるとスラー／グリッサンドが**黙って消える** 🔴

スラーは相手を `{ noteId, pitchKey }` で覚えている（app.js:1617）。`pitchKey` は「音名+オクターブ」。

音高を変える経路は3つあり、**どれも `pitchKey` を更新しない**:

| 操作 | 該当箇所 |
|---|---|
| ドラッグで音高変更 | app.js:1814 `tone.key = key` |
| ↑↓キー | app.js:2211 `tone.key = transposeKey(...)` |
| 移調ボタン | app.js:3314 `tone.key = transposeKey(...)` |

相手側の音高が変わると `keys.findIndex(t => pitchId(t.key) === tone.slurTo.pitchKey)` が -1 になり、スラーが消える。

**特に移調は致命的**で、範囲移調するとその範囲のスラー・グリッサンドが全部飛ぶ。

---

## A-6. コピー＆ペーストしたスラーが**元の音符を指し続ける**

```js
// app.js:3371 / 3446 / 2141
notes.map((n) => ({ ...structuredClone(n), id: makeNoteId() }))
```

`id` は新しく振り直すが、コピー元の `tone.slurTo.noteId` / `glissandoTo.noteId` は**そのまま**。
結果、ペーストした音符から**コピー元の位置まで**スラー／グリッサンドの線が引かれる。楽譜をまたいで長い線が走る。

`tieToNext` も残るので、A-3 と組み合わさって想定外の場所に繋がる。

---

## A-7. テンプレート読み込みで**クラッシュする** 🔴

```js
// app.js:3722  score をオブジェクトリテラルで組み直している
score = {
  title, composer, lyricist, timeSig, keySignature, pickupBeats, clefs, measures
};
// bpm / bpmNoteValue / timeSigDisplay / showChordSymbols /
// measureWidthScale / instruments / shapes / titleStyle … が全部無い
syncControlsFromScore();
```

`syncControlsFromScore`（app.js:462）が

```js
instrumentSelect.value = score.instruments[part] || DEFAULT_INSTRUMENT;
```

を実行して **`TypeError: Cannot read properties of undefined (reading 'upper')`** で落ちる。
`renderShapes` も `score.shapes` を舐めるので同様。

**「テンプレート → 新規作成」は現状ほぼ確実に動かない。**
`createEmptyScore()` をベースに上書きする形に直すべき。

---

## A-8. 連符を**解除する手段がない**

`note.tupletId` を設定するコードは2か所（`insertNote` app.js:1980、`applyMultiTuplet` app.js:3652）あるが、**null に戻すコードがどこにも存在しない**。

一度3連符にしたら、その音符を削除して置き直すしかない。

---

## A-9. n番括弧（volta）が再生に**一切反映されない**

v0.5.0 で追加した1番/2番括弧は `measure.volta` に持っているが、`playOrder.js` の `buildPlayOrder` は `volta` を一度も参照しない。

したがって:
- 1回目も2回目も1番括弧を通る
- 2番括弧は1回目にも鳴る

**記譜はできるが演奏順は間違ったまま。** MIDI/WAV書き出しも同じ。

---

## A-10. Coda マーカーが再生で無視される

`MARKERS` に `'Coda'` があり、UIからも設定できるが、`buildPlayOrder` は `Segno` / `D.C.` / `D.S.` / `Fine` しか見ていない。
「D.S. al Coda → To Coda で飛ぶ」が実現できず、Coda は完全に飾り。

---

## A-11. 和音へのタイが同じ拍に**重複した音符を作る**

`applyTieToSelectedTone`（app.js:2406〜）は、タイ先が無いとき音符を自動生成する。和音の2音目以降にタイを付けると `partialChordNote: true` の音符が追加され、コード自身のコメントがこう認めている:

> it briefly means this beat and the chord's other real continuation note overlap in the written rhythm

つまり**同じ拍に2つの音符が並ぶ不正な状態**を意図的に作っている。指摘のあった「和音にタイを付けて編集すると記号がバグる」はここが原因。

さらに悪いことに、`migrateScore` は保存ファイルから `partialChordNote` を**削除する**（app.js:122 `delete n.partialChordNote;`）のに、`applyTieToSelectedTone` は今も設定する。

→ **保存して開き直すと、同じ操作でも挙動が変わる**（マージされず別音符が作られる）し、`playback.js:103` の分岐も効かなくなってタイが切れる。

また、自動生成される音符の音価は**リボンで今選ばれている音価**（`selectedDuration`）であって、元の音符の音価ではない。

---

## A-12. 選択ハイライト・キャレットが**印刷／PDFに出る**

`style.css:501` の `@media print` が隠しているのは
`.quickbar` `.ribbon-tabs` `.ribbon-panel` `.floating-toolbox` `.context-menu` `.measure-number-label` のみ。

隠されていない:
- `.range-highlight`（範囲選択の青い箱、style.css:206）
- `.insert-caret`（点滅カーソル、style.css:217）
- `.playing-highlight`（再生位置の箱、style.css:233）

**さらに**、選択中の音符は VexFlow 側で青く塗られている（`staffRenderer.js:291` `setKeyStyle(idx, { fillStyle: '#1a73e8' })`）ため、
**選択したまま印刷すると、その音符だけ青く印刷される。**

`beforeprint` で選択を解除して再描画する処理が無い（grep で 0件）。

---

## A-13. タイトルを変えても1ページ目のタイトルが更新されない

```js
// app.js:2887
titleInput.addEventListener('change', () => {
  score.title = titleInput.value || '無題の楽譜';
  pushHistory();          // ← render() が無い
});
```

`renderTitleHeader` は `render()` からしか呼ばれないので、クイックアクセスバーでタイトルを変えても、譜面上のタイトルは次に何か別の操作をするまで古いまま。

---

## A-14. MIDI書き出しが楽器設定・拍子・調号を落とす

`midiExport.js`:
- **プログラムチェンジ（音色）を書いていない** → `score.instruments` で選んだ音色が反映されず、書き出したMIDIは全部ピアノ
- **拍子・調号のメタイベントが無い** → DAWで開くと常に4/4・ハ長調
- ソート比較関数が不整合:
  ```js
  midiEvents.sort((a, b) => a.tick - b.tick || (a.type === 'off' ? -1 : 1));
  ```
  `a` しか見ておらず、`compare(a,b)` と `compare(b,a)` が両方 -1 になりうる（同tick同type）。実害は出にくいが仕様違反。

---

## A-15. 強弱記号・アーティキュレーションが**音に一切反映されない**

`playback.js:187` / `247`、`midiExport.js:48` すべて `velocity: 100` 固定。

- `note.dynamic`（pp〜ff）→ 無視
- `note.hairpin`（クレッシェンド）→ 無視
- `note.articulation`（スタッカート、テヌート、アクセント…）→ 無視（音長も変わらない）

「記号」タブで付けられるものが全部**見た目だけ**になっている。

---

## A-16. コード再生用のパーサが完全なデッドコード

`scoreModel.js:319` の `parseChordSymbol` は「コード表記を再生に反映するため」とコメントされているが、**どこからも import されていない**（grep で 0件）。コード記号は鳴らない。

---

## A-17. 譜表の当たり判定が固定値で、譜表間隔を広げるとズレる

```js
// staffRenderer.js:1156 findHitRegion
y >= r.topY - 40 && y <= r.topY + 80
// app.js:721 candidateRegionsAt
y >= r.topY - 45 && y <= r.topY + 85
```

`staveGap` は加線の量に応じて `computeRequiredStaveGap` で**動的に広がる**のに、当たり判定の帯は固定。

- 間隔が広がると、譜表と譜表の間に**どこにも当たらない死角**ができる
- 逆に既定の100pxでは帯が `topY-45 〜 topY+85`（130px）で、次の譜表は `topY+100` から始まる → **20px以上重なる**。`nearestRegion` で中心線の近い方を選んで緩和しているが、加線の多い音符では誤爆する

---

## A-18. 1ページ4段固定で、譜表間隔が広がると**ページ下端からはみ出す**

```js
// staffRenderer.js:57  linesPerPage: 4（固定）
// :740  lineY = topMargin + pageTitleExtra + (line - lineStart) * (staveGap*2 + systemGap + rehearsalBandHeight + voltaOffset)
```

既定値での計算: `110 + 70 + 4×(100×2 + 80 + 29) = 1416` に対しページ高 `1485` → ぎりぎり収まる。

しかし `staveGap` は**楽譜全体で一番加線が多い音符1つ**を基準に、**全システムに一律で**広げられる（staffRenderer.js:181〜209）。
`staveGap` が 130 になると `180 + 4×369 = 1656 > 1485` で**はみ出す**。volta があるとさらに +20/段。

はみ出しの検査も、`linesPerPage` を減らす処理も無い。

---

## A-19. 休符はクリックしただけで音符に変わってしまう

```js
// app.js:1860  mouseup
if (dragging.wasRest && dragging.note.isRest) {
  dragging.note.isRest = false;   // ← moved を見ていない
  setStatus('休符を音符にしました');
}
```

`dragging.moved` を条件に入れていないので、**ドラッグせずクリックしただけでも変換される**。
結果、**休符を「選択」する手段が無い** — 音価を変えたい／削除したいだけでも、いったん音符になってしまう。

---

## A-20. 編集しながら再生すると、ハイライトが古い座標に出る

`schedulePlaybackHighlights`（app.js:2697）は再生開始時に `findNoteHitById` で `hitMap` の要素を掴んで `setTimeout` に閉じ込める。
再生中に編集して `render()` が走ると `hitMap` は作り直されるが、予約済みタイマーは**古い座標オブジェクト**を持ったまま発火する。

加えて `render()` は `container.innerHTML = ''` でページごと作り直すので、表示済みハイライトのDOMも消える（`highlightEls` には残骸参照が残る）。

---

## A-21. 保存JSONに選択状態が混入する

`markSelection()`（app.js:361）はスコアの各音符に `selected` / `selectedKeyIndex` / `multiSelectedKeyIndices` / `rangeSelected` を**書き込む**。
`saveScoreFile`（app.js:2837）は `JSON.stringify(score)` をそのまま出す。

→ 保存ファイルにUI状態が入り込み、ファイルが無駄に膨らむ。開き直したとき残留した選択状態が一瞬見える。

---

## A-22. `beatsFitExcludingNote` と `cascadeOverflow` の**挙動不一致**

- 音価変更（`applyDurationToSelectedNote` app.js:2256）→ 入らなければ**拒否**して「この小節にはこれ以上入りません」
- 音符挿入（`insertNote` app.js:1989）→ **黙って次の小節へ押し出す**（`cascadeOverflow`）

同じ「あふれ」なのにユーザーから見て挙動が正反対。
なお `cascadeOverflow` 上のコメントは「Finale/Sibelius/Dorico/MuseScoreも同様にリフローする」と書いているが、**Finaleについては誤り**（マニュアル確認済み。Finaleはダイアログで4択を出す。詳細は `Finale比較・改善リスト.md` §6）。

---

## A-23. `doRangePaste` が容量を無視して上書きする

```js
// app.js:3371
score.measures[targetIndex][clipboard.part] = notes.map(...);
```

`cascadeOverflow` を呼ばないので、通常小節の内容を**弱起小節（容量が短い）に貼る**と即座に容量超過する。

---

# B. 構成・設計上の問題

## B-1. `render()` が副作用を持っている

```js
// app.js:395
function render() {
  markSelection();        // ← スコアにUI状態を書き込む
  recomputeAutoChords();  // ← スコアのコード欄を書き換える
  const result = renderScore(...);
  ...
}
```

「描画」関数がモデルを2種類とも書き換えている。実害:

- `pushHistory(); render();` の順で呼ぶ箇所（`insertNote` app.js:1991 など）では、**履歴に入るスナップショットのコードが古い**。次のレンダで直るので見た目には出ないが、undo/redo で一瞬ズレる
- `render(); pushHistory();` の順の箇所（mouseup app.js:1873）と混在していて、どちらが正しいのか読み手に伝わらない

**修正方針:** `recomputeAutoChords()` はモデル更新側（音符の追加・削除・音高変更）で呼び、`markSelection()` は描画時にモデルを触らず引数で渡す。

## B-2. `render()` が毎回すべてのページを作り直す

`renderScore` は先頭で `container.innerHTML = ''` して、全ページ分の SVG を `Renderer` で作り直す。
これが呼ばれるのは:

- **音高ドラッグ中の mousemove ごと**（app.js:1815）
- **ズームボタンの長押し中 100ms ごと**（app.js:2604 → `setZoom` → `render()`）。ズームは CSS transform を変えるだけなのに全再描画している
- 範囲ドラッグ中の mousemove ごと（app.js:1804）

10ページの楽譜では 1操作あたり10個の SVG ツリーを毎フレーム再構築することになる。**編集が重くなる主因。**

最低限、ズームは `render()` を呼ばず transform だけ更新すべき。

## B-3. 保存が Blob ダウンロード方式で、**上書き保存が存在しない**

```js
// app.js:2836
function saveScoreFile() {
  download(`${score.title || 'score'}.json`, ...);
}
```

- 保存先を選べない（常に既定のダウンロードフォルダ）
- 開いたファイルのパスを覚えていないので、保存のたびに `score.json` `score(1).json` `score(2).json` … と増える
- **PDF書き出しだけは Electron のネイティブ保存ダイアログを使っている**（main.js の `export-pdf`）ので、アプリ内で流儀が2つある

Electron なのだから `dialog.showSaveDialog` / `showOpenDialog` に寄せ、`最近使ったファイル`・`上書き保存(Ctrl+S)` を持たせるべき。

## B-4. 「開く」「テンプレート」が未保存確認をしない

終了時（`close-requested`）は確認モーダルを出すのに、`btn-open`（app.js:2845）と `btn-load-template`（app.js:3714）は**現在のスコアを無言で捨てる**。
`savedHistoryIndex` の仕組みは既にあるので、同じ判定を挟むだけで防げる。

## B-5. 定数がモデルとUIで二重定義されている

`scoreModel.js` から export されているのに**どこからも import されていない**もの:

`DYNAMICS` / `ARTICULATIONS` / `ARTICULATION_LABELS` / `MARKERS` / `BARLINE_OPTIONS` / `BARLINE_LABELS` / `TUPLET_COUNTS` / `PART_LABELS` / `parseChordSymbol`

実際に使われているのは `index.html` の `<select>` にベタ書きされた `<option>` と、`app.js` 内の別定義（`DYNAMIC_OPTIONS` app.js:3454、`ARTICULATION_OPTIONS` app.js:3461）。

**すでに食い違っている:**

| | scoreModel | 実際に使われている定義 |
|---|---|---|
| アーティキュレーション | 4種（`ARTICULATIONS`） | **10種**（`ARTICULATION_OPTIONS`）|

片方だけ直して不整合が広がる典型的な形。どちらかに寄せる。

## B-6. `app.js` が3,800行の単一ファイル

1ファイルに、モデル移行・履歴・マウス処理・キーボード・リボン全部・図形エディタ・再生UI・ファイル入出力・コンテキストメニュー・モーダルが同居している。

自然な分割単位:
`history.js` / `selection.js`（selected・multiSelected・noteRangeSelection・rangeSelection の4系統）/ `mouse.js` / `ribbon.js` / `shapes.js` / `fileIO.js`

**特に選択状態が4種類あって相互作用が複雑**（`clearSelection` / `clearRangeSelection` / `clearMeasureSelection` / `multiSelected = []` の呼び分けが各所に散っている）ので、ここだけでも1モジュールに切り出す価値がある。

## B-7. 履歴が毎回スコア全体のディープコピー

```js
// app.js:341
history.push(structuredClone(score));
```

上限も無い。長い楽譜を長時間編集するとメモリが単調増加する。上限（例: 100手）を入れるか、差分方式にする。

## B-8. 印刷／PDFのページサイズが指定されていない

- `style.css` に **`@page` ルールが無い**（grep で0件）。`.score-page` は 1050×1485 CSS px = 実寸 約27.8cm×39.3cm で、A4に収まらない。ブラウザ任せの縮小がかかる
- `main.js` の `printToPDF({})` も**オプション完全省略** → 既定は Letter。`pageSize: 'A4'` も `printBackground` も `preferCSSPageSize` も指定していない

`A4_RATIO` でページの縦横比だけ合わせているのに、出力側でA4を指定していないので意味が活きていない。

## B-9. テストが1つも無い

`package.json` に test スクリプトが無く、テストファイルも存在しない。

**純粋関数で今すぐテストできるもの**（DOM非依存）:
`beatsPerMeasure` / `measureCapacity` / `noteBeats` / `cascadeOverflow` / `buildPlayOrder` / `buildPlaybackEvents` / `detectChordCandidates` / `transposeKey` / `migrateScore`

A-1（拍子の分母）も A-9（volta）も、`buildPlayOrder` と `beatsPerMeasure` にテストが数本あれば入り込まなかった類のバグ。

## B-10. `migrateScore` が壊れたファイルに対して無防備

`loaded.timeSig` / `loaded.measures[i].upper` 等を検証していない。
`timeSig` が欠けたファイルを開くと `beatsPerMeasure` の `score.timeSig.split` で落ちる。
`try/catch` は「読み込みに失敗しました」を出すが、その時点で `score` が半分書き換わっている可能性がある（`score = migrateScore(loaded)` を先に代入しているため）。

---

# C. UX上の引っかかり

| # | 内容 | 該当箇所 |
|---|---|---|
| C-1 | undo/redo すると選択が必ず外れる（`selected = null`） | app.js:349, 357 |
| C-2 | 終止線を設定すると**確認なしで**以降の小節が全削除される。100小節の3小節目に付けたら97小節消える（undoで戻せるが警告が無い） | app.js:3205 |
| C-3 | 和音に同じ音名・同じオクターブの音を臨時記号付きで足せない（`alreadyThere` が臨時記号を無視）。C4がある位置に♯を付けて置くと「すでに同じ高さの音があります」で弾かれる | app.js:526 |
| C-4 | ♯/♭ボタンが「次に置く音の設定」と「選択中の音符の編集」を兼ねていて、押した瞬間に既存音符が書き換わる | app.js:2508 |
| C-5 | 歌詞が常にページ左端（`pageMargin`）に描かれ、設定した小節位置と無関係 | staffRenderer.js:1034 |
| C-6 | クレッシェンド開始だけ置いて終了を置かないと、何も描かれず警告も出ない | staffRenderer.js:910 |
| C-7 | `linesPerPage: 4` が固定で、ユーザーが1ページの段数を選べない | staffRenderer.js:57 |
| C-8 | volta が重なって設定できてしまう（既存の範囲と重複チェックが無い） | app.js:3685 |
| C-9 | 連符に別々の音価の音符を指定できてしまう（検証が「連続していること」のみ） | app.js:3647 |
| C-10 | WAV書き出しがモノラル固定、かつ全曲を1つの `OfflineAudioContext` に載せるので長い曲でメモリを食う | playback.js:233 |
| C-11 | 再生が1音1 `setTimeout`。音数×3個（再生1＋ハイライト2）のタイマーが並び、タイミングが揺れる | playback.js:185, app.js:2700 |
| C-12 | Electron のメニューを丸ごと消しているので、Ctrl+P などOS標準の操作が効かない（リボンのボタン経由のみ） | main.js:`Menu.setApplicationMenu(null)` |

---

# 修正の優先順位（提案）

## 第1弾 — 「壊れている」ものを直す

1. **A-1** 拍子の分母（`beatsPerMeasure` の1行 + `Voice` の `beat_value`）
2. **A-7** テンプレート読み込みのクラッシュ（`createEmptyScore()` ベースに変える）
3. **A-3/A-4/A-5/A-6** リンクの寿命管理 — **1か所にまとめる**のが要点:
   - 「音符を消す」「音高を変える」「複製する」を通る唯一の関数を作り、そこで `tieToNext` / `slurTo` / `glissandoTo` を検証・更新・破棄する
   - タイも `slurTo` と同じ**明示リンク**（`tieTo: { noteId }`）に変えれば、A-3の「遠くに繋がる」問題は構造的に消える
4. **A-12** 印刷前に選択解除（`beforeprint` で `clearSelection()` → `render()`）

## 第2弾 — 使っていて破綻するもの

5. **A-2 + A-22** 小節容量の一貫した扱い（`Finale比較・改善リスト.md` §6 の3点セット）
6. **A-9/A-10** volta と Coda の再生対応
7. **A-11** 和音タイの重複音符（`partialChordNote` を廃止し、タイ先の音符を1つに正規化する）
8. **A-8** 連符の解除
9. **A-19** 休符はクリックで選択、変換は明示操作に

## 第3弾 — 構成の立て直し

10. **B-1** `render()` から副作用を追い出す
11. **B-2** ズームを再描画から切り離す／ドラッグ中の再描画を間引く
12. **B-3/B-4** ファイル入出力を Electron ダイアログに統一し、上書き保存と未保存確認を入れる
13. **B-9** 純粋関数にテストを入れる（ここまで直したものの再発防止）
14. **B-6** `app.js` の分割（特に選択状態まわり）
15. **A-14/A-15** 強弱・音色を再生とMIDIに反映

---

## 補足: コード中の誤ったコメント（修正済み）

`cascadeOverflow` の直前に

> same as inserting a note mid-measure in mainstream notation software (Finale, Sibelius, Dorico, MuseScore) reflows everything after it instead of simply refusing the insertion

とあったが、**Finaleについてはこの記述は正しくない**。Finaleは超過時にダイアログで4択（そのまま／削除／次の小節へ／全部詰め直す）を出し、詰め直しは `Utilities ▸ Rebar` という明示コマンドでもある。マニュアル該当箇所は `Finale比較・改善リスト.md` §6 に引用済み。

→ コメントを事実に合わせて書き直し、あわせて `reportOverflow()` で「何個の音符を次の小節に送ったか」を通知するようにした（Finaleの4択のうち最も影響の大きい選択肢を無言で既定にしていた状態の解消）。
