# Finale 2010 マニュアル vs 本アプリ（v0.5.0）差異整理

元ネタ: **Finale 2010 for Windows User Manual**（2,821ページ / 見出し1,183項目）
比較対象: 本アプリ v0.5.0（Electron + VexFlow 5、`src/renderer/` 約7,000行）

マニュアルの構成:
1. What's New
2. **Encyclopedia**（A–Z の機能事典、約700項目・941の操作手順）← 機能の実体はほぼここ
3. Reference（Appendix / **ダイアログ約620個** / Glossary / キーボードショートカット / メニュー全項目 / **プラグイン約100個** / **ツール27個**）
4. Worksheets

---

## 1. Finaleの設計思想（＝「使用感」の正体）

真似すべきかどうかの判断のために、まず何がFinaleの操作感を作っているかを分解する。

| # | 思想 | 中身 |
|---|---|---|
| 1-1 | **ツール中心（Tool-based）** | Main Tool Palette に27個のツール。「今どのツールを持っているか」でクリックの意味が変わる。`Esc` または `Ctrl+Shift+A` でいつでも万能の **Selection Tool** に戻る |
| 1-2 | **入力方式が3系統** | **Simple Entry**（マウス＋PCキーボード＋キャレット）/ **Speedy Entry**（小節に編集フレームを出して数字キーで音価指定）/ **HyperScribe**（MIDI鍵盤のリアルタイム録音） |
| 1-3 | **キャレット入力** | ワープロのカーソルと同じ。音価＝テンキー1–8、音高＝A–G または ↑↓、`Enter`で確定、`0`/`Tab`で休符。**入力直後の音符は自動的に選択状態**になり、そのまま修飾キーで編集できる |
| 1-4 | **Metatools（1キー割り当て）** | `Shift`＋任意キー で記号を登録 → そのキーを押しながらクリックで一発挿入。アーティキュレーション/強弱/連符/拍子/調号/クレフ/コード/リピート/スラー等ほぼ全部に用意 |
| 1-5 | **選択の階層** | 「部分小節を含む範囲」と「全パートの完全小節＝**Stack**」を明確に区別。ダブルクリック/`Alt`クリックで段階的に拡張、`Shift+←→`で伸縮、`Shift+End/Home`で右/左全部 |
| 1-6 | **カテゴリ + ライブラリ + Document Options** | 強弱・テンポ・発想標語・奏法・リハーサル記号を**カテゴリ**で管理し、フォント/位置を一括変更（既に置いた記号にも遡って適用）。体裁は全部ダイアログで数値指定、ライブラリとして保存/読込 |
| 1-7 | **Smart Shape** | 伸縮する記号（スラー/ヘアピン/8va/グリッサンド/括弧/カスタム線）。ダブルクリック＋ドラッグで作成、ハンドルで整形、**改行をまたぐと自動で2つに割れる** |
| 1-8 | **レイアウトの二層構造** | 音符の詰め方（Music Spacing）と、行・ページの割り付け（Measure Group / System Lock / Fit Measures / Update Layout）が別レイヤー |
| 1-9 | **Human Playback** | 楽譜上の記号（rall., ヘアピン, トレモロ, スタッカート…）を解釈して人間らしく再生。スタイル（Romantic/Classical/Jazz）選択可 |
| 1-10 | **多声部** | 1段につき **4 Layer × V1/V2 = 8声部**。レイヤーごとにステム方向・休符位置・MIDIチャンネルを設定 |

---

## 2. 本アプリの現状（v0.5.0）

- 3段譜（上鍵盤/下鍵盤/ペダル）固定、パート別クレフ（ト音/ヘ音のみ）
- 入力: 譜面クリック→キャレット表示→同じ場所を再クリックで確定。ドラッグで音高変更
- 音価: 全〜16分＋付点、休符5種、臨時記号♯♭♮、連符3/5/7
- 記号: タイ / スラー / グリッサンド / 強弱pp〜ff / クレッシェンド・デクレッシェンド / アーティキュレーション10種（スタッカート〜アルペジオ、フェルマータ・トリル・ターン・モルデント含む）
- 小節: 追加/削除/位置指定挿入、小節線3種、リピート、マーカー（Segno/D.C./D.S./Fine/Coda）、n番括弧（volta）
- 注記: コード（自動検出＋手動）、リハーサル記号、レジストレーション M1–M16、歌詞（段・パート単位の1行テキスト）
- 調号: 長調/短調 各13（♯6〜♭6）。拍子: 曲全体で1種類
- 編集: 範囲選択（1パート内）、コピー/ペースト/削除、度数単位の移調、Undo/Redo
- 表示: 縦スクロール / 見開き、ズーム、譜面幅スライダー、作業用小節番号
- 挿入: テキストボックス・四角・円・線・矢印（ページ固定）
- 出力: JSON保存/読込、印刷・PDF、MIDI、WAV、テンプレート
- 再生: パート別GM音色、選択位置から再生、再生位置ハイライト
- キーボード: `Delete`/`Backspace`、`Ctrl+Z/Y/C/X/V`、`↑↓`（選択音の全音階移動）、`Esc` **のみ**

---

## 3. 使用感（操作モデル）の差異

**★ = 使用感の差として特に効いている箇所**

| 項目 | Finale | 本アプリ | 差 |
|---|---|---|---|
| ツール概念 | ツール持ち替え式。Escで選択ツール | 常時「配置モード」＋リボン | **真似しなくてよい**。今の方が明快 |
| ★音価の指定 | テンキー `1`–`8`（+`Ctrl+Alt+Shift`+数字） | ボタンクリックのみ | ショートカットが皆無 |
| ★音高の指定 | `A`–`G`キー / `↑↓`でキャレット移動 / MIDI鍵盤 | 譜面のクリック位置のみ | クリック必須 |
| ★確定 | `Enter` | 同じ場所を2回クリック | キーボードだけで完結しない |
| ★カーソル移動 | `←→`で音符単位、`Ctrl+←→`で小節単位、`Ctrl+↑↓`で和音内/上下段 | なし | 連続入力のリズムが作れない |
| 和音の積み方 | 数字 `1`–`8` で音程指定（3で3度上）、`Shift`+数字で下方向、`Shift`+A–G | 同じ位置にもう一度クリック | 度数で積めない |
| 休符 | `0` / `Tab`、`Ctrl`+音価キー | 休符ボタンを選んでからクリック | モード切替が要る |
| 既存音符の音価変更 | `Alt`+数字 | 音符を選択→音価ボタン（実装済み ✔） | ほぼ同等。キーだけ無い |
| 音符↔休符トグル | `R` | 休符クリック→音符化は可、逆は不可 | 片方向 |
| 臨時記号 | `=`(♯) `-`(♭) `N`(♮)、`+/-`で半音上下、`\`で異名同音 | ♯♭♮を事前選択 | 事後付与ができない |
| タイ | `T`（次へ）/ `Shift+T`（前へ）、`Ctrl+F`で向き反転 | ボタン（実装済み ✔） | 向き反転なし |
| ステム反転 | `L` / `Shift+L`で既定に戻す | **なし** | 自動のみ |
| 連桁の分割・結合 | `/`、`Shift+/`で既定に戻す | **なし** | 自動のみ |
| 記号の即挿入 | Metatools（キー＋クリック） | リボン/右クリックメニュー | 概念自体がない |
| ★挿入 vs 上書き | Paste（置換）と Insert（押し出し）を明確に区別。Speedyには「Insert Notes or Rests」モード | 上書き/末尾追加のみ | **途中に音符をねじ込めない** |
| 範囲選択 | ドラッグ / `Shift+←→↑↓` / `Shift+End,Home` / 段の左をクリックで1段全部 / `Ctrl+A` / Select Regionダイアログ（画面外も指定可）/ Stack概念 | ドラッグのみ・1パート内のみ | 複数パート同時が不可 |
| 右クリック | 対象ごとに文脈メニュー（小節・記号・コード・歌詞・クレフ…） | **かなり近い形で実装済み ✔** | 良い |
| Undo | Undo/Redo リストで「ここまで一気に戻す」 | 1段ずつ | 軽微 |
| ★拍子・調号の途中変更 | 小節を指定して範囲変更。カーテシー（行末予告）表示も自動 | **曲全体で1種類のみ** | 大きい |
| ★行・ページの割り付け | 1行あたりの小節数指定（Fit Measures）、強制改行、システム間隔、システムのロック、Update Layout | 幅スライダー＋`lineBreak`フラグ（UI未露出） | 手動の追い込みができない |
| 記号の位置微調整 | ハンドルをドラッグ / 矢印キーでナッジ / `Backspace`で既定位置に戻す | 図形のみ可、楽譜記号は不可 | 中 |
| 歌詞 | 音符ごとの**音節割り当て**（ハイフン・メリスマ・ワード延長線） | 段・パート単位の1行テキスト | 見た目の整合が取れない |

---

## 4. 取り入れるべき機能リスト（優先度順）

### 優先度A — 「エレクトーン楽譜として実用」に効く

| # | 項目 | 理由・内容 |
|---|---|---|
| A-1 | **曲の途中での拍子変更** | 現在は曲全体1種類。実曲でほぼ必ず要る。Finale同様「この小節から」の範囲指定。行頭に予告表示（カーテシー）も |
| A-2 | **曲の途中での調号変更** | 同上。転調のある曲が書けない |
| A-3 | **キーボード入力（キャレット拡張）** | 音価＝数字キー、音高＝A–G / `↑↓`、`Enter`確定、`0`で休符、`←→`で音符移動・`Ctrl+←→`で小節移動、`R`で音符↔休符、`Alt`+数字で音価変更。**これ1つで入力速度の体感が最も変わる** |
| A-4 | **音符の途中挿入（Insert）** | 現状は上書き/追記のみ。「ここに1拍ねじ込んで後ろをずらす」ができない。ペーストにも Paste / Insert の2種を |
| A-5 | **8va / 8vb（オクターブ記号）** | エレクトーンの上鍵盤・ペダルで頻出。加線だらけを回避できる。Smart Shape方式（開始音符→終了音符をクリック）で既存のスラー/グリッサンド実装を流用可 |
| A-6 | **行・ページ割り付けの手動制御** | 「1行あたり◯小節」の指定と「この小節から新しい行」の明示UI（`lineBreak`は既にモデルにあるので露出させるだけ）。ページ跨ぎの見栄え調整に必須 |
| A-7 | **ステム方向の手動反転／連桁の手動分割・結合** | 自動任せだと読みにくい箇所が必ず出る。VexFlow側は対応済みなのでフラグを持たせるだけ |
| A-8 | **リピート整合チェック（Check Repeats相当）** | 現在の「D.C./D.S.後は内側リピートを取らない」簡略ロジックの取りこぼしを、演奏順プレビュー（例: 1→8, 1→4, 9→16）として見せる |
| A-9 | **装飾音符（grace note）** | 実曲で頻出。Finaleは`Alt+G`でトグル |
| A-10 | **記号の位置微調整** | 強弱・コード・リハーサル記号などをドラッグ/矢印キーで動かし、`Backspace`で既定位置に戻す |

### 優先度B — 効果は大きいが実装コストも大きい

| # | 項目 | メモ |
|---|---|---|
| B-1 | **同一段内の2声部（レイヤー相当）** | 上鍵盤でメロディ＋内声、下鍵盤で伴奏形が割れるケース。Finaleの4層×V1/V2はやり過ぎ。**「1段あたり2声、ステム上/下固定」に絞れば**現実的 |
| B-2 | **半音単位の移調（調号自動変更つき）** | 現在は度数のみ。Finaleは「調号を変える移調」と「音程だけの移調」を分けている |
| B-3 | **歌詞の音節割り当て方式** | 音符に1音節ずつ、ハイフンとメリスマ延長線。今の行テキスト方式から作り替えになる |
| B-4 | **MusicXML 入出力** | 他ソフトとの互換。読み込みは実装重、書き出しだけ先でも価値あり |
| B-5 | **MIDIファイル取り込み → 自動採譜** | Finaleは量子化設定＋Split Point（左右手の分割音）で2段に振り分ける。エレクトーンなら3段への振り分けルールが要る |
| B-6 | **音楽スペーシングの調整** | 最小/最大小節幅、音符間隔。今の幅スライダーの発展形 |
| B-7 | **リハーサル記号の自動連番** | A, B, C… を自動採番。今は手動選択 |
| B-8 | **検索と置換** | 音型の検索・置換（Finaleの Search and Replace / SmartFind and Paint） |
| B-9 | **テンプレート/スタイルの拡充** | Finaleの Document Styles / Library 相当。今の「テンプレート」を、フォント・記号の既定値まで持てる形に |
| B-10 | **ページ番号・ヘッダー/フッター・タイトルページ** | 印刷物としての体裁 |

### 優先度C — あれば嬉しい

- Metatools 相当（よく使う記号を数字キーに割り当てて1キー挿入）
- 複数パート同時の範囲選択（Finaleの Stack）
- Undo/Redo リスト（複数段まとめて戻す）
- テンポ変化の記号（accel./rall.）と再生への反映
- 複数小節休符（エレクトーンでは使用頻度低め）
- 上下鍵盤をまたぐ音符（Cross-staff）

---

## 5. 取り入れなくてよいもの（元ネタにはあるが切って良い）

方針「シンプルに完成させる」に沿って、明確に落とす領域:

- **ギター関連一式** — タブ譜、フレットボード図、ベンド、ハンマリング/プリング、カポ
- **打楽器関連一式** — Percussion Map、Percussion Layout Designer、各種ドラム音源マップ
- **SmartMusic 連携**（Finaleの機能のかなりの割合を占めるが完全に無関係）
- **楽譜スキャン（SmartScore Lite）** — OMR
- **HyperScribe / Transcription Mode** — リアルタイム演奏録音・量子化。ただし「MIDI鍵盤で音高だけ指定するステップ入力」は A-3 の延長として検討価値あり
- **Linked Parts / パート譜抽出 / Score Merger** — エレクトーンは1人1譜面
- **VST / SoundFont / Garritan / Mixer / Audio Track / SMPTE同期**
- **MIDI Tool**（生MIDIデータの数値編集: キーベロシティ、コンティニュアスデータ、Start/Stop Time）
- **Shape Designer**（記号の自作エディタ）と Executable Shape
- **プラグイン約100種**（Piano Reduction, Explode/Implode, Canonic Utilities, Chord Morphing…）
- **移調楽器 / Concert Pitch表示 / Neutral Key**
- **Ossia / Mirror / Note Mover / Cutaway / Staff Styles / 五線数の変更**
- **図形音符（Shape Note）、微分音、非標準調号、複合拍子**
- **Human Playback の詳細設定**（スタイル別辞書、区間ごとのON/OFF）
- **Batch Printing / Booklet印刷 / タイル印刷 / EPS・PostScript出力**

---

## 6. 小節の容量管理 — 現状 vs Finale

### 現状（本アプリ）

`app.js` の `cascadeOverflow()`（1922行〜）が、小節の容量を**ソフトな折り返し点**として扱っている:

- 小節に入りきらない音符は、**黙って**次の小節の先頭へ送られる
- 送った先でもあふれたら、さらに次へ — 連鎖的に後ろへ波及する
- 末尾で足りなくなれば小節を自動追加する

一方、**音価の変更**（`applyDurationToSelectedNote` / `applyDottedToSelectedNote`）は容量チェックで拒否し「この小節にはこれ以上入りません」と出す。
→ **同じ「あふれ」なのに、挿入は黙って流し、音価変更は拒否する**という不整合がある。

> ⚠️ `cascadeOverflow()` の上のコメントは「Finale/Sibelius/Dorico/MuseScoreも同じように後続をリフローする」と書いているが、**Finaleについてはこれは正しくない**（下記）。

### Finaleの実際の仕様

Finaleは小節を**容量を持った器**として扱う。Speedy Entryで拍数を超えると、その小節を離れた時点で
**"There Are Too Many Beats In This Measure"** ダイアログが出て、**ユーザーに処理を選ばせる**:

| 選択肢 | 挙動 |
|---|---|
| Leave the measure alone | そのまま残す（カデンツァ等の意図的な超過用） |
| Delete the extra notes | あふれ分を削除（最後の音価を切り詰めてでも） |
| Move the extra notes to the next measure | **次の小節へ送る（1段だけ）**。小節線をまたぐタイが付くことも |
| Keep moving the extra notes until all measures contain the correct number of beats | **連鎖的に送り続ける** ← 今のアプリの `cascadeOverflow` と同じ |

さらに:

- このチェック自体を Speedyメニューの **Check for Extra Notes** でON/OFFできる
- 「小節をまたいで詰め直す」は **Utilities ▸ Rebar** という**明示的なコマンド**としても独立している（範囲を選んで実行）。拍子を変更したときは Rebar Music チェックボックスで自動実行
- 「1小節に無制限に音符を入れる」ための専用手順（*To permit unlimited notes in one measure*）も用意されている

### 結論と推奨

**「エラーで追加できない」ではなく「黙ってやらない」が正解。** 方向性としてはユーザーの読みどおりで、
今の自動あふれ送りは変えたほうがいい。理由:

- 1音の挿入ミスで曲の後半全体が1拍ずつズレる事故が起きうる（Undo1回で戻せるとはいえ怖い）
- 「この小節は今何拍埋まっているか」が編集中に見えなくなる
- 音価変更との挙動の不整合が消える

具体的には3点セットで:

1. **既定は拒否** — 挿入時も `beatsFitExcludingNote` と同じチェックをかけ、「この小節にはあと◯拍しか入りません」と出して**入れない**（音価変更と挙動を揃える）
2. **あふれた時に選ばせる** — Finaleの4択を簡略化して「入れない / 次の小節へ送る / 後ろ全部を詰め直す」の3択ダイアログ。「今後は聞かない」チェックボックス付き
3. **明示コマンドとしての「詰め直し（Rebar）」** — 範囲を選んで手動実行。拍子を変えたときにも走らせるか確認する

加えて、意図的な超過を許すために**小節属性「拍数チェックを無視」**（カデンツァ用）を持たせておくと逃げ道になる。

---

## 7. 取り入れるべき項目リスト（32件・優先度順）

**優先度A = 実用上ほぼ必須 / B = 効果大だがコスト大 / C = あれば嬉しい**

### 7-1. 小節・入力の土台（ここが使用感の核）

| # | 優先 | 項目 | 内容 |
|---|---|---|---|
| 1 | **A** | 小節容量の厳格化 | 自動あふれ送りをやめ、挿入時も容量チェック。あふれ時は3択ダイアログ（§6） |
| 2 | **A** | 詰め直し（Rebar）コマンド | 範囲選択して「小節をまたいで詰め直す」を明示実行 |
| 3 | **B** | 小節属性「拍数チェック無視」 | カデンツァ等の意図的超過を許す逃げ道 |
| 4 | **A** | 残り拍数の表示 | 選択中の小節に「あと◯拍」を出す。1と対で効く |
| 5 | **A** | 数字キーで音価選択 | `1`〜`5` で全音符〜16分音符。`.`で付点 |
| 6 | **A** | A–Gキーで音高入力 | キャレット位置から最も近い同名音に入る（Finale方式） |
| 7 | **A** | Enterで確定入力 | 現在の「2度クリック」に加えてキーボードでも確定 |
| 8 | **A** | ←→でカーソル移動 | 音符単位。`Ctrl+←→`で小節単位。連続入力のリズムを作る |
| 9 | **A** | `0`で休符入力 | 音価はそのまま、休符として置く（モード切替不要に） |
| 10 | **A** | `R`で音符↔休符トグル | 現状は休符→音符の片方向のみ |
| 11 | **A** | `Alt`+数字で既存音符の音価変更 | 選択→数字キー。今はボタンのみ |
| 12 | **B** | 度数キーで和音を積む | `3`で3度上、`Shift+3`で3度下。今は同じ位置に重ねクリック |
| 13 | **A** | 音符の途中挿入 | 後続を押し出して割り込ませる（1・2とセットで設計する） |
| 14 | **A** | Insertペースト | 貼り付けを「置換」と「押し出し挿入」の2種に分ける |
| 15 | **A** | 臨時記号の事後付与 | 選択した音符に♯♭♮を後から付ける。`+`/`-`で半音上下 |
| 16 | **C** | 異名同音の切り替え | F♯↔G♭ をキー一発で |

### 7-2. 記譜機能

| # | 優先 | 項目 | 内容 |
|---|---|---|---|
| 17 | **A** | 曲の途中での拍子変更 | 小節範囲を指定して適用。現状は曲全体1種類 |
| 18 | **A** | 曲の途中での調号変更 | 同上。転調のある曲が書けない |
| 19 | **B** | カーテシー（行末予告）表示 | 拍子・調号が変わる直前の行末に予告を出す |
| 20 | **A** | 8va / 8vb | エレクトーンで頻出。既存のスラー/グリッサンドの2点指定UIを流用可 |
| 21 | **A** | 装飾音符（grace note） | 実曲で頻出 |
| 22 | **A** | ステム方向の手動反転 | 現状は自動任せ。VexFlow側は対応済み |
| 23 | **A** | 連桁の手動分割・結合 | 同上。読みやすさに直結 |
| 24 | **C** | タイの向き反転 | 和音や2声部で必要になる |
| 25 | **B** | 1段内2声部 | 上鍵盤のメロディ＋内声など。Finaleの4層×V1/V2はやり過ぎなので**「1段2声、ステム上/下固定」に絞る** |
| 26 | **B** | 半音単位の移調 | 調号を自動で変える移調。現状は度数のみ |

### 7-3. レイアウト・体裁

| # | 優先 | 項目 | 内容 |
|---|---|---|---|
| 27 | **A** | 行割り付けの明示UI | 「1行◯小節」指定と「この小節から改行」（`lineBreak`はモデル済み・UI未露出） |
| 28 | **A** | 記号の位置微調整 | ドラッグ/矢印キーでナッジ、`Backspace`で既定位置に戻す |
| 29 | **B** | 音楽スペーシング調整 | 最小/最大小節幅。今の幅スライダーの発展形 |
| 30 | **B** | ページ番号・ヘッダー/フッター | 印刷物としての体裁 |

### 7-4. 注記・確認・その他

| # | 優先 | 項目 | 内容 |
|---|---|---|---|
| 31 | **A** | リピート整合チェック | 演奏順プレビュー（例: 1→8, 1→4, 9→16）。現在の簡略ロジックの取りこぼしを可視化 |
| 32 | **B** | 歌詞の音節割り当て | 音符ごとに1音節、ハイフン＋メリスマ延長線。今の行テキスト方式から作り替え |
| 33 | **C** | リハーサル記号の自動連番 | A, B, C… を自動採番 |
| 34 | **C** | Metatools相当 | よく使う記号を数字キーに割り当てて1キー挿入 |
| 35 | **C** | 複数パート同時の範囲選択 | Finaleの Stack（全パートの完全小節）概念 |

---

## 8. 着手順の提案

依存関係があるので、この順番だと手戻りが少ない:

1. **#1〜4（小節容量）** — ここを先に決めないと、#13（途中挿入）も#14（Insertペースト）も設計が固まらない
2. **#5〜11（キーボード入力）** — 単独で完結し、体感の変化が最も大きい
3. **#17〜18（拍子・調号の途中変更）** — データモデルに手が入るので、入力系が固まってから
4. **#20〜23（8va・装飾音符・ステム・連桁）** — 描画層だけで閉じる。並行して進められる
5. **#27〜28（レイアウト）** — 仕上げ段階
